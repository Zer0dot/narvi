//! `narvi-tray` — `ksni` StatusNotifierItem. Client of the daemon like any other.
//!
//! M6: register the tray item; click toggles the GUI; menu = presets + Quit.

use anyhow::Result;

fn main() -> Result<()> {
    eprintln!(
        "narvi-tray {} — not yet implemented (M6)",
        env!("CARGO_PKG_VERSION")
    );
    Ok(())
}
